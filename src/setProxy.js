/*
const { createProxyMiddleware } = require("http-proxy-middleware");

module.exports = function (app) {
  app.use(
    "/api",
    createProxyMiddleware({
      target: "http://localhost:8080", //# 서버 URL or localhost:설정한포트번호
      changeOrigin: true,
    })
  );
};

//프록시란 ? 클라이언트가 다른 네트워크를 간접적으로 접근하게 해주는것
*/
